global SbitmapArr=
(
	getClassInstances Bitmaptexture target:sceneMaterials
)
for f in SbitmapArr do
(
	cc=((getFilenamePath xx)+"qqdd"+(getFilenameType xx))  
	xx = f.filename
	renameFile xx cc
	f.filename = ""
	f.filename =cc
) 
这个就可以改场景内的贴图名和重命名，任何地方的都可以